name: Dependabot Discord Notification

on:
  pull_request:
    types: [opened]
  push:
    branches:
      - main

jobs:

  # ---------------------------------------------------------------
  # Notify Discord when Dependabot opens a major version update PR.
  # Major updates require manual review and are never auto-merged.
  # Minor and patch updates are handled silently by auto-merge.
  # ---------------------------------------------------------------
  notify-major-update:
    name: Major Update Opened
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]' && github.event_name == 'pull_request'
    steps:
      - name: Fetch Dependabot PR metadata
        id: metadata
        uses: dependabot/fetch-metadata@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify Discord of major update requiring review
        if: steps.metadata.outputs.update-type == 'version-update:semver-major'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          curl -H "Content-Type: application/json" \
            -d '{
              "embeds": [{
                "title": "⚠️ Dependabot Major Update — Review Required",
                "color": 16744272,
                "fields": [
                  {
                    "name": "Update",
                    "value": "'"${PR_TITLE}"'",
                    "inline": false
                  },
                  {
                    "name": "PR",
                    "value": "[#'"${PR_NUMBER}"']('"${PR_URL}"')",
                    "inline": true
                  }
                ],
                "footer": {
                  "text": "antwanchild/docs • '"$(date +'%B %-d, %Y')"'"
                }
              }]
            }' \
            "$DISCORD_WEBHOOK_URL"

  # ---------------------------------------------------------------
  # Notify Discord when a Dependabot PR is auto-merged into main.
  # Only fires if a Dependabot PR was merged within the last 5 minutes,
  # which tightly correlates this notification with the triggering push.
  # Skips silently if the push was not from a Dependabot merge.
  # ---------------------------------------------------------------
  notify-auto-merged:
    name: PR Auto-Merged
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Find Dependabot PR merged in the last 5 minutes
        id: merged-pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Calculate a cutoff timestamp 5 minutes ago
          CUTOFF=$(date -u -d '5 minutes ago' +%Y-%m-%dT%H:%M:%SZ 2>/dev/null \
            || date -u -v-5M +%Y-%m-%dT%H:%M:%SZ)

          # Find the most recently merged Dependabot PR and check if it falls within the window
          MERGED_PR=$(gh pr list --state merged --base main \
            --json author,title,number,mergedAt,url \
            --jq '[.[] | select(.author.login == "dependabot[bot]") | select(.mergedAt >= "'"$CUTOFF"'")] | sort_by(.mergedAt) | last')

          if [ -z "$MERGED_PR" ] || [ "$MERGED_PR" == "null" ]; then
            echo "No Dependabot PR merged in the last 5 minutes — skipping notification"
            echo "found=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          {
            echo "title=$(echo "$MERGED_PR" | jq -r '.title')"
            echo "number=$(echo "$MERGED_PR" | jq -r '.number')"
            echo "url=$(echo "$MERGED_PR" | jq -r '.url')"
            echo "found=true"
          } >> "$GITHUB_OUTPUT"

      - name: Notify Discord of auto-merged Dependabot PR
        if: steps.merged-pr.outputs.found == 'true'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          PR_TITLE: ${{ steps.merged-pr.outputs.title }}
          PR_NUMBER: ${{ steps.merged-pr.outputs.number }}
          PR_URL: ${{ steps.merged-pr.outputs.url }}
        run: |
          curl -H "Content-Type: application/json" \
            -d '{
              "embeds": [{
                "title": "✅ Dependabot PR Auto-Merged",
                "color": 3066993,
                "fields": [
                  {
                    "name": "Update",
                    "value": "'"${PR_TITLE}"'",
                    "inline": false
                  },
                  {
                    "name": "PR",
                    "value": "[#'"${PR_NUMBER}"']('"${PR_URL}"')",
                    "inline": true
                  },
                  {
                    "name": "Merged to",
                    "value": "'"${{ github.ref_name }}"'",
                    "inline": true
                  }
                ],
                "footer": {
                  "text": "antwanchild/docs • '"$(date +'%B %-d, %Y')"'"
                }
              }]
            }' \
            "$DISCORD_WEBHOOK_URL"
